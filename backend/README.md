# 🏠 REDWW Real Estate API Backend

## 📋 نظرة عامة

Backend Node.js احترافي لموقع العقارات REDWW مع دعم:

- **39,366 وحدة عقارية**
- **298 مشروع**
- **112 شركة تطوير**
- **23 منطقة جغرافية**
- **13 نوع عقاري**

---

## 🚀 البدء السريع

### 1️⃣ التثبيت

```bash
cd backend
npm install
```

### 2️⃣ تحضير البيانات

```bash
cd ..
python data_processor.py
```

### 3️⃣ تشغيل الـ Server

```bash
cd backend
npm start
# أو للـ Development
npm run dev
```

### ✅ التحقق من التشغيل

```
GET http://localhost:3000/api/health
```

---

## 📡 الـ Endpoints الرئيسية

### 🏘️ الوحدات (Units)

```
GET    /api/units                          # جميع الوحدات
GET    /api/units?page=1&limit=20         # مع الـ Pagination
GET    /api/units/:id                     # وحدة محددة
GET    /api/units?minPrice=1000000        # تصفية حسب السعر
GET    /api/units?bedrooms=3              # تصفية حسب الغرف
GET    /api/units/project/:projectId      # وحدات المشروع
```

**مثال الرد:**

```json
{
  "success": true,
  "data": [
    {
      "id": 92731,
      "name": "Villa",
      "price": 225280000,
      "currency": "EGP",
      "bedrooms": "4",
      "area_sq_m": 394,
      "area_name": "North Coast",
      "project_name": "Ogami",
      "developer_name": "Sodic"
    }
  ],
  "pagination": {
    "total": 39366,
    "page": 1,
    "limit": 20,
    "pages": 1969
  }
}
```

### 🏢 المشاريع (Projects)

```
GET    /api/projects                       # جميع المشاريع
GET    /api/projects/:id                  # مشروع محدد + وحداته
GET    /api/projects/developer/:id        # مشاريع الشركة
```

### 🏭 الشركات (Developers)

```
GET    /api/developers                     # جميع الشركات
GET    /api/developers/:id                # شركة + مشاريعها
```

### 📍 المناطق (Areas)

```
GET    /api/areas                          # جميع المناطق
GET    /api/areas/:id                     # منطقة + إحصائيات
```

### 🏷️ الأنواع (Types)

```
GET    /api/types                          # جميع الأنواع
GET    /api/types/:id/units               # وحدات من نوع محدد
```

### 🔍 البحث (Search)

```
GET    /api/search?q=villa                # البحث عن 'villa'
GET    /api/search?q=north%20coast        # البحث عن 'north coast'
GET    /api/search?q=sodic&page=1         # البحث مع الـ Pagination
```

---

## 📊 الإحصائيات

من السكريبت تم إنشاء البيانات:

| المقياس        | القيمة          |
| -------------- | --------------- |
| إجمالي الوحدات | 39,366          |
| المشاريع       | 298             |
| الشركات        | 112             |
| المناطق        | 23              |
| متوسط السعر    | 17.6 مليون جنيه |
| أقل سعر        | 1 مليون جنيه    |
| أعلى سعر       | 385 مليون جنيه  |

---

## 🔧 Filtering Examples

### تصفية متقدمة

```
GET /api/units?minPrice=1000000&maxPrice=10000000&bedrooms=3&area_id=1
```

**المعاملات:**

- `minPrice`: الحد الأدنى للسعر
- `maxPrice`: الحد الأقصى للسعر
- `bedrooms`: عدد الغرف (2, 3, 4, 5, etc)
- `area_id`: معرف المنطقة
- `type_id`: معرف النوع
- `page`: رقم الصفحة (افتراضي: 1)
- `limit`: عدد النتائج لكل صفحة (افتراضي: 20)

---

## 📂 هيكل المشروع

```
backend/
├── server.js           # ملف الـ Server الرئيسي
├── package.json        # Dependencies
├── .env               # متغيرات البيئة
└── routes/
    ├── units.js       # endpoints الوحدات
    ├── projects.js    # endpoints المشاريع
    ├── developers.js  # endpoints الشركات
    ├── areas.js       # endpoints المناطق
    ├── types.js       # endpoints الأنواع
    └── search.js      # endpoint البحث

data_processor.py      # معالج البيانات
api_data_ready.json    # البيانات المعدة
```

---

## 🔐 Response Format

جميع الـ Responses تتبع نفس الهيكل:

```json
{
  "success": true,
  "data": [],
  "pagination": {
    "total": 0,
    "page": 1,
    "limit": 20,
    "pages": 0
  }
}
```

---

## ⚙️ المتطلبات

- **Node.js** >= 14
- **Python** >= 3.7
- **npm** أو **yarn**

---

## 📝 الملاحظات المهمة

✅ البيانات محملة من الذاكرة (بسرعة عالية)
✅ دعم الـ Pagination الكامل
✅ تصفية متقدمة
✅ بحث سريع
✅ CORS مفعل
✅ معالجة الأخطاء كاملة

---

## 🎯 الخطوات التالية

1. ✅ تحميل البيانات
2. ✅ API Endpoints الأساسية
3. 📋 إضافة Authentication (JWT)
4. 📋 إضافة Database (MongoDB)
5. 📋 إضافة Caching (Redis)
6. 📋 تطبيق Frontend (React)

---

## 📧 Support

للمساعدة أو الأسئلة، تواصل معنا! 💪
